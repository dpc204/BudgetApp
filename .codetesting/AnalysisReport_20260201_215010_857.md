# Test Failures due possible code bugs

## Budget.ApiTests.csproj - Budget.Api.Features.Transactions.UnitTests.InsertTransactionsTests.AddMultipleTransactions_NullList_ThrowsArgumentNullException
- **Confidence**: High
- **Test File**: Budget.ApiTests\Features\Transactions\InsertTransactionsTests.cs
- **Bug Location**: Budget.Api\Features\Transactions\InsertTransactions.cs@104

### Analysis
The test correctly expects ArgumentNullException when passing null to AddMultipleTransactions, which is the standard practice for null parameter validation. However, the production code at line 104 attempts to iterate over the null list without first validating it, causing a NullReferenceException instead. The production code should include parameter validation at the beginning of the method to throw ArgumentNullException when list is null.

### Suggested Fix
The AddMultipleTransactions method in InsertTransactions.cs needs to add null parameter validation at the beginning of the method (after line 101, before line 102). Add the following:

```csharp
if (list == null)
  throw new ArgumentNullException(nameof(list));
```

Or using C# 10+ syntax:

```csharp
ArgumentNullException.ThrowIfNull(list);
```

This will ensure that when a null list is passed, the method throws an ArgumentNullException with proper context, rather than letting it fail with a NullReferenceException during iteration.

## Budget.ApiTests.csproj - Budget.Api.Features.Transactions.UnitTests.InsertTransactionsTests.AddSingleTransaction_WithNullCommand_ThrowsArgumentNullException
- **Confidence**: High
- **Test File**: Budget.ApiTests\Features\Transactions\InsertTransactionsTests.cs
- **Bug Location**: Budget.Api\Features\Transactions\InsertTransactions.cs@79

### Analysis
The test correctly expects ArgumentNullException when a null command is passed to AddSingleTransaction. However, the production code does not validate the input parameter before accessing its properties. At line 83 in InsertTransactions.cs, the code attempts to access 'request.Trans' without first checking if 'request' is null. This causes a NullReferenceException instead of the expected ArgumentNullException. The production code needs to add parameter validation at the beginning of the AddSingleTransaction method to throw ArgumentNullException when the request parameter is null.

### Suggested Fix
The AddSingleTransaction method in InsertTransactions.cs (line 79) needs to add null parameter validation. Add the following check at the beginning of the method, right after the method signature and before any other code:

```csharp
if (request == null)
  throw new ArgumentNullException(nameof(request));
```

The fixed method should look like:

```csharp
public async Task<TransactionAddResult> AddSingleTransaction(AddNewTransaction.Command request)
{
  if (request == null)
    throw new ArgumentNullException(nameof(request));

  await BeginBatchAsync();

  var rslt = await AddTransactionAsync(request.Trans);

  _InsertTransactionResult.SingleAddedTransaction = rslt.Adapt<TransactionDto>();
  await UpdateEnvelopeBalancesAsync();
  await EndBatchAsync();
  return _InsertTransactionResult;
}
```

